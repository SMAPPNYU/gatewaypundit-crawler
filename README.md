# gatewaypundit-crawler
An automated system to crawl live traffic from The Gateway Pundit

# Usage
python version >= 3.7
pip install selenium

To start collect most up-to-date tweets, run
`python crawler.py`
